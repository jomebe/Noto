# OpenAI API Configuration for AI-powered hover insights.
# Keep this server-side only. On Vercel, set it as an Environment Variable.
OPENAI_API_KEY=sk_test_your_api_key_here

# Optional: Specify the OpenAI model to use in /api/insight.
OPENAI_MODEL=gpt-4o-mini
